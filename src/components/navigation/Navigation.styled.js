import styled from 'styled-components';
import { StyledLink } from 'components/appBar/AppBar.styled';

export const LeftNavLink = styled(StyledLink)`
  &:first-of-type {
    margin-right: 32px;
  }
`;
