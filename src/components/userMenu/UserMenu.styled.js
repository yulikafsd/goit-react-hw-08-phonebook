import styled from 'styled-components';
import { StyledLink } from 'components/appBar/AppBar.styled';

export const RightNavLink = styled(StyledLink)`
  &:last-of-type {
    margin-left: 32px;
  }
`;
